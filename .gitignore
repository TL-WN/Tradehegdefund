# --- dependencies (installed locally for the tunnel; not needed on Render) ---
node_modules/
npm-debug.log*

# --- python ---
__pycache__/
*.pyc
.venv/

# --- local runtime (not deployed) ---
public_url.txt
tunnel.out
chrome-debug/
.env

# --- runtime data: COMMITTED so the cloud dashboard stays populated ---
# (the GitHub Action regenerates + commits these on schedule)
# meeting_logs/, news_logs/, research_logs/ are kept in git.
# paper_book.json, history.json, market_snapshot.json are kept in git.

# --- exclude any stray local-only json not part of the deploy ---
briefing.txt
